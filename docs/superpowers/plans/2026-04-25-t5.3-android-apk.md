# T5.3 Android APK Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Android WebView wrapper that loads the web UI.

**Architecture:** Android app with WebView that loads localhost:8080.

**Tech Stack:** Kotlin, Android SDK, WebView

---

## File Structure

```
android/
├── app/src/main/
│   ├── java/com/prometheus/app/MainActivity.kt
│   └── res/layout/activity_main.xml
└── AndroidManifest.xml
```

---

## Task 1: Android Project Files

**Files:**
- Create: `android/AndroidManifest.xml`
- Create: `android/app/src/main/java/com/prometheus/app/MainActivity.kt`
- Create: `android/app/src/main/res/layout/activity_main.xml`

- [ ] **Step 1: Write AndroidManifest.xml**

```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prometheus.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Prometheus"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
```

- [ ] **Step 2: Write MainActivity.kt**

```kotlin
package com.prometheus.app

import android.os.Bundle
import android.webkit.WebView
import android.webkit.WebViewClient
import androidx.appcompat.app.AppCompatActivity

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        
        val webView = WebView(this).apply {
            settings.javaScriptEnabled = true
            settings.domStorageEnabled = true
            webViewClient = object : WebViewClient() {
                override fun shouldOverrideUrlLoading(view: WebView?, url: String?): Boolean {
                    view?.loadUrl(url ?: "http://localhost:8080")
                    return true
                }
            }
            loadUrl("http://localhost:8080")
        }
        
        setContentView(webView)
    }
}
```

- [ ] **Step 3: Write activity_main.xml**

```xml
<?xml version="1.0" encoding="utf-8"?>
<WebView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/webview"
    android:layout_width="match_parent"
    android:layout_height="match_parent" />
```

- [ ] **Step 4: Commit**

---

## Task 2: Build Verification

- [ ] **Step 1: Verify Go build**

Run: `go build ./...`

- [ ] **Step 2: Note about Android SDK**

Note: Building APK requires Android SDK (`./gradlew assembleDebug`). This is a template only.

- [ ] **Step 3: Commit**

---

## Acceptance Criteria

- [ ] AndroidManifest.xml with WebView and INTERNET permissions
- [ ] MainActivity.kt loads web UI
- [ ] Go build passes
- [ ] Source ready for Android SDK build