# Vision

The vision layer is scaffolded with three modes:

- local provider placeholder
- cloud-capable interface seam
- no-op fallback provider

Cross-platform screen capture helpers are present in `internal/vision/capture.go`.

