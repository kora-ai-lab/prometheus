# Capabilities

The capability engine scaffold covers:

- registry-based capability lookup
- install recipe selection by detected package manager
- future forge support for missing tools

Current registry examples live in `internal/capabilities/registry.go`.
