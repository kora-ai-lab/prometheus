# Android Guide

This scaffold reserves Android/Termux as a primary target.

Planned coverage:

- Linux ARM64 build output suitable for Termux userland
- Offline-first runtime flow
- Model placement under `~/.prometheus/models`
- Future embedded llama-server ARM64 artifact wiring

