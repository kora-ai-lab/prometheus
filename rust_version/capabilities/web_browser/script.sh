#!/bin/bash
# This is a wrapper script for the web_browser capability.
# Since we have a Rust implementation, the core loop will invoke it via Rust.
# This script serves as a placeholder for the USE_CAPABILITY protocol requirement.

echo "This capability is implemented internally in Rust. Use the WebDriver interface."
exit 0
